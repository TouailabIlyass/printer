
        <div>
        <form action="{{url('categories')}}" method="POST">
            @include('categories.formCategorie')
            <button type="submit" >Add</button>
        </form>
        </div>
